# Пак раздаётся по хешам из index.toml, посчитанным от файлов как они лежат на диске.
# Любая конвертация переводов строк меняет байты в git и ломает проверку хеша у игроков
# (packwiz-installer скачивает файл с GitHub и сверяет sha256). Поэтому — никаких
# преобразований: что в рабочем дереве, то и в репозитории.
* -text
